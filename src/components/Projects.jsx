export default function Projects() {
  return (
    <section
      id="projects"
      className="observe opacity-0 flex flex-col gap-3 transition-all ease-in-out duration-500 translate-x-0
     bg-slate-900 rounded-lg bg-clip-padding backdrop-filter backdrop-blur-md bg-opacity-20 p-5 shadow-lg"
    >
      <h2 className="font-bold text-2xl text-teal-300">Projects</h2>
    </section>
  );
}

function ProjectCard({ title, description, image, skills }) {
  return <div></div>;
}
